{
  "devDependencies": {
    "@jahed/terraform": "^1.0.1",
    "terrafile": "^0.1.12"
  },
  "scripts": {
    "clean-project": "cd submodules/tf_modules/project && rm -Rf vendor",
    "build-project": "cd submodules/tf_modules/project && terrafile install",
    "clean-env": "cd submodules/tf_modules/env && rm -Rf vendor",
    "build-env": "cd submodules/tf_modules/env && terrafile install",
    "clean-ou": "cd submodules/tf_modules/ou && rm -Rf vendor",
    "build-ou": "cd submodules/tf_modules/ou && terrafile install",
    "clean-snapshots": "cd submodules/snapshots && npm run clean",
    "build-snapshots": "cd submodules/snapshots && npm run build",
    "clean-terraform": "cd terraform && rm -Rf vendor",
    "build-terraform": "cd terraform && terrafile install",
    "clean": "npm run clean-project && npm run clean-env && npm run clean-ou && npm run clean-snapshots && npm run clean-terraform",
    "build": "npm run build-project && npm run build-env && npm run build-ou && npm run build-snapshots && npm run build-terraform",
    "init": "cd terraform && terraform init",
    "plan": "cd terraform && terraform plan",
    "terrafile": "npm run clean && npm run build",
    "all": "npm run terrafile && npm run init && npm run plan"
  }
}
